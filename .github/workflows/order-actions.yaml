name: Build and Push Image For Order

on:
  push:
    branches:
      - "**"

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      imageRepository: "globaltickets.order"
      containerRegistry: "globalticketsacr.azurecr.io"
      dockerfilePath: "code/ordering/src/Ordering.WebApi/Dockerfile"
      deploymentFile: "order.yaml"
      namespace: "globaltickets"

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Print Current Directory
        run: pwd

      - name: List All Files in Repository
        run: ls -R

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Re-Tag Previous Latest
        run: |
          set -e
          if docker pull ghcr.io/${{ github.repository }}/order:latest; then 
            echo "Successfully pulled :latest image, proceeding to re-tag."
            docker tag ghcr.io/${{ github.repository }}/order:latest ghcr.io/${{ github.repository }}/order:${{ github.run_number }}
            docker push ghcr.io/${{ github.repository }}/order:${{ github.run_number }}
          else
            echo "No existing :latest image to re-tag"
          fi

      - name: Build an image from Dockerfile
        run: |
          docker build -t ghcr.io/${{ github.repository }}/order:latest -f ${{ env.dockerfilePath }} ${{ github.workspace }}

      - name: Push image to GitHub Container Registry
        run: |
          docker push ghcr.io/${{ github.repository }}/order:latest
